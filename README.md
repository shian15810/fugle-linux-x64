# fugle-linux-x64

```bash
$ nativefier "https://www.fugle.tw/?p=linux&v=${FUGLE_APP_DESKTOP_VERSION}" --name "Fugle" --inject "./fugle-app-prerequisite/not-for-app.css" --icon "./fugle-app-prerequisite/fugle.png" --platform "linux" --arch "x64"
```
